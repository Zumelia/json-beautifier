# `web/` — jsonbeautifier.dev

**Status: planned.** Nothing here yet.

Source of the product website: landing page plus the online JSON tools (beautify, validate,
minify, tree view). The tools run entirely in the browser on top of `core/` — nothing is
uploaded, there is no backend, and the site is served as static files.

Published under the same MIT license as the rest of the repository.
